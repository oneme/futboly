export enum Position {
    TOP,
    LEFT,
    RIGHT,
    BOTTOM
}

export enum BoxPosition {
    START = 1,
    FINE = 5,
    PARKING = 21,
    GO_TO_BENCH = 33,
    CLUB_DAY = 39,
    LAST_BOX = 40,
    BENCH = 13,
}

export enum PropertyType {
    GENERAL,
    TRIBUNE,
    DEALERSHIP,
    CLUB_SERVICES,
}

export enum DepartmentType {
    PLAIN,
    FEDERATION,
    SURPRISE,
}